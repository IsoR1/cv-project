/* eslint-disable function-paren-newline */
import React, { Component } from "react";

class Header extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <h1 className="h1-header">CV Builder</h1>;
  }
}

export default Header;
